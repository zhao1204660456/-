window.onload = function() {
    var banner = document.querySelector('.banner');
    var box = banner.children[0];
    var boaUl = box.children[0];
    var img = boaUl.children;
    console.log(img);
}